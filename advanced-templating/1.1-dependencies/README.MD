# dependencies

- use dependencies to control when resources are deployed.
- IE: A public IP depending on a NIC to be deployed.
- Can slow down deployments. It's intention is not to document resource dependencies.
